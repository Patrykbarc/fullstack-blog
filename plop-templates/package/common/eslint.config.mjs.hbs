// @ts-check
import baseConfig, { defineConfig } from '@monorepo/eslint-config';

export default defineConfig(
	{
		ignores: ['eslint.config.mjs', '.prettierrc.mjs', 'dist/**'],
	},
	...baseConfig,
);
